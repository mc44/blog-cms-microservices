# Blog CMS frontend

Next.js 15 app that talks to the API **gateway** only.

## Dev

```bash
cd frontend
npm install
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080 npm run dev
```

Sign in with the seeded user (`user@example.com` / `change-me` from Compose), then create posts and upload images.
