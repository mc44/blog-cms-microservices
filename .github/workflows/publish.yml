name: Publish images

on:
  push:
    branches: [main, master]

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  DOCKERHUB_NAMESPACE: marcfajardo44

jobs:
  publish-cms:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - folder: 1-gateway-service
            image: gateway-service
          - folder: 2-blog-service
            image: blog-service
          - folder: 4-media-service
            image: media-service
          - folder: 5-audit-service
            image: audit-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - run: mvn -B verify -DskipTests -f ${{ matrix.folder }}/pom.xml
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.folder }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ matrix.image }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ matrix.image }}:latest
            ${{ env.DOCKERHUB_NAMESPACE }}/${{ matrix.image }}:${{ github.sha }}
            ${{ env.DOCKERHUB_NAMESPACE }}/${{ matrix.image }}:latest

  publish-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install
        working-directory: 3-frontend
      - run: npm run build
        working-directory: 3-frontend
        env:
          NEXT_PUBLIC_GATEWAY_URL: http://localhost:8080
          NEXT_PUBLIC_TENANT_ID: blog-cms
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: 3-frontend
          push: true
          build-args: |
            NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
            NEXT_PUBLIC_TENANT_ID=blog-cms
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/blog-cms-frontend:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/blog-cms-frontend:latest
            ${{ env.DOCKERHUB_NAMESPACE }}/blog-cms-frontend:${{ github.sha }}
            ${{ env.DOCKERHUB_NAMESPACE }}/blog-cms-frontend:latest

  deploy-vps:
    needs: [publish-cms, publish-frontend]
    runs-on: ubuntu-latest
    if: vars.VPS_DEPLOY_ENABLED == 'true'
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/blog-cms/0-deploy
            export GATEWAY_IMAGE_TAG=${{ github.sha }}
            export BLOG_IMAGE_TAG=${{ github.sha }}
            export MEDIA_IMAGE_TAG=${{ github.sha }}
            export AUDIT_IMAGE_TAG=${{ github.sha }}
            export FRONTEND_IMAGE_TAG=${{ github.sha }}
            ./scripts/deploy.sh
