# Optional overrides for `0-deploy/optional/all-in-one/docker-compose.yml` only.
# Copy to a local file (e.g. .env.local) if you pass vars into that compose — never commit secrets.
#
# For the normal Docker workflow (VPS or local), use 0-deploy/.env instead:
#   cp 0-deploy/.env.example 0-deploy/.env

AUTH_JWT_SECRET=change-me-in-env-please-change-me-in-env
AUTH_REFRESH_TOKEN_HMAC_KEY=change-refresh-hmac-key
BLOG_TENANT_ID=blog-cms

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

KAFKA_ENABLED=false
